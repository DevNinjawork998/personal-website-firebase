Run the test suite with coverage and summarize the results.

Steps:
1. Run `npm run test:coverage`
2. Summarize the coverage report: overall line/branch/function percentages
3. Call out any files with notably low coverage (below 50%) that are not mocks or config files
4. Do not list every file — focus on the summary and any gaps worth addressing
