Run the TypeScript compiler in no-emit mode to check for type errors across the entire project:

```bash
npx tsc --noEmit
```

Report all type errors found. If there are no errors, confirm the project is type-clean. Do not fix errors unless the user asks.
